export default (theme) => ({
    root: {
        display: 'inline-block',
        cursor: 'pointer',
        lineHeight: 0,
    },
    input: {
        display: 'none',
    },
    disabled: {
        cursor: 'not-allowed',
    },
});
