export default (theme) => ({
    root: {
        cursor: 'pointer',
    },
    input: {
        display: 'none',
    },
    disabled: {
        opacity: 0.5,
        cursor: 'not-allowed',
    },
});
