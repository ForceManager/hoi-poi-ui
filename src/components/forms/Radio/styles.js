export default (theme) => ({
    root: {
        display: 'inline-block',
        cursor: 'pointer',
        lineHeight: 0,
    },
    input: {
        display: 'none',
    },
    disabled: {
        opacity: 0.5,
        cursor: 'not-allowed',
    },
    checked: {},
});
